# Supabase Postgres – Connection String
# Supabase Dashboard → Project Settings → Database → Connection string
# Für Vercel / Next.js API-Routen: "Transaction" pooler (Port 6543) empfohlen.
# Format: postgresql://postgres.<project-ref>:<password>@aws-0-<region>.pooler.supabase.com:6543/postgres
DATABASE_URL=

# Optional: Google Places API Key (sonst nur OpenStreetMap als Quelle)
GOOGLE_API_KEY=

# Single-Password-Auth (Cookie-basiert)
APP_PASSWORD=
APP_SECRET=

# Sales-Hub-Webhook (optional) – pusht Leads bei Statuswechsel auf
# "interested"/"call_scheduled" an den Volles-Studio-Sales-Hub.
# URL = der scraper-leads-Endpoint des Hubs, SECRET wird als x-webhook-secret
# gesendet (vom Hub geprüft). Bleibt leer = Feature aus (No-Op).
SALESHUB_WEBHOOK_URL=
SALESHUB_WEBHOOK_SECRET=
# SALESHUB_WEBHOOK_TIMEOUT=3500

# Maschinen-Token: erlaubt dem Sales Hub, per "Authorization: Bearer <token>"
# Leads zu LESEN + Enrichment (Bewertungen etc.) on demand zu triggern.
# Nur Lese-/Enrichment-Endpoints, keine Mutationen. Leer = aus.
SCRAPER_API_TOKEN=

# Cal.com-Terminplaner: der "Termin via Cal.com buchen"-Button öffnet diesen
# öffentlichen Buchungslink (vorausgefüllt mit Name + E-Mail des Leads).
# Kein API-Key nötig – Cal.com macht Kalendereintrag + Mail selbst.
# Default ist https://cal.com/volles-studio/30min; hier nur zum Übersteuern.
# NEXT_PUBLIC_CALCOM_BOOKING_URL=https://cal.com/volles-studio/30min

# WhatsApp-Nachricht "mit allen Infos" (Button am Lead). Diese zwei Links werden
# in die Nachricht eingesetzt; leer = die jeweilige Zeile entfällt.
# NEXT_PUBLIC_ONBOARDING_FORM_URL=
# NEXT_PUBLIC_DEMO_VIDEO_URL=
